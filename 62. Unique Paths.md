## Step1

毎度右か下かを選択するので、DFSで探索してゴールに至る道をカウントしていく方法が思いついた。
時間計算量がパッと出てこない。
全てのマスで2つの選択があるので、時間計算量はO(2^mn) になる。とても遅い。
ステップ数が2^(10^4) = 10^(3000) ぐらいなので、実行時間は大体 10^(3000) / 10^8 = 10^(2992) sec と現実的ではない長さになる。
小さいテストケースで動くことを確認した。

15分すぎたが、もう少し考えてみる。
* 一番上の行は、右に移動することでのみ到達できる。
* 一番左の列は、下に移動することでのみ到達できる。
* それ以外のマスは、一つ上からか左からのみ到達できる。

お風呂に入ったらDPの解法を思いついた。
マスを一つづ見ていって、左と上のマスの経路の数を足していく。
時間計算量は O(mn) で、ステップ数は 10^2 * 10^2 = 10^4 になるので、実行時間 = 10^4 / 10^8 = 0.1 ms ぐらいになる。

## Step2

* https://github.com/Shunii85/arai60/pull/33/changes
* テーブルではなくて、一つの列を使う方法
* テーブルの方が読みやすいと思った

```cpp
current_column[row] += current_column[row - 1];
```
この一行で、テーブルの左と上の経路を足しているのが分かりづらいからかなと思った

```cpp
        int above_paths = paths[col];
        paths[col] = paths[col - 1] + above_paths;
```
みたいに書けば分かりやすくなるかな？

* https://github.com/Shunii85/arai60/pull/33/changes
* DFS
の解法でキャッシュを使うことで、一つのマスの経路を計算するのは一回で済む。だから時間計算量はO(mn)

一番書きやすく、コードの意味も伝わりやすいと思うので、テーブルを使うDPの解法にした。

かかった時間:
* step1:
  1. 15min
  2. 13min
* step2
  * 一つの行のみを保持する解法 6min
  * DFS with cache 9min
* step3
  * 5min
  * 3min
  * 2min
