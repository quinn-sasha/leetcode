## Step1

### Memo

nums = [2,7,9,3,1]
pattern:
* 2, 9, 1
* 7, 3
* 9, 1 <- 見る必要ない

nums[i] は非負の整数なので、奇数の添字と偶数の添字の２パターンの大きい和の方を選択すればいい。

nums = [2,1,1,2]
上記の解法は間違っていた。別に奇数の位置と偶数の位置の組み合わせもある。
nums の要素数が奇数だけの場合は、元の解法でも動く。

まずは、各位置で選択するかしないかを決めて、選択したらindexを2つ進めるというのが正しいナイーブな実装を思いついた。
以前解いたsubsequenceの問題に、数字が隣り会えないという制約を加えた問題と理解した。
robbed_moneys[i] に nums[i] を含んだ和の最大値が入るとする。
robbed_moneys[i]は、indexが1番目と2番目の位置では、そのままnums[i]の数字になる。
3番目以降は、一つ左を除く、今まで見てきた和の内の最大のものと現在の数字を足したものになる。
計算量は O(N^2) (N: nums.length)になる。最大ステップ数が N^2 = 10^4 なので、実行時間 = 10^4 / 10^8 = 0.1msぐらいになる。

## Step2

* https://github.com/Shunii85/arai60/pull/35/changes
* > 前の人までの最大と、前の人が盗みに入らなかったときの最大を使う。
* Step1のコードだと、隣を除く今までに盗んだ額の内の最大値をループで探していたが、その必要はなかった
* この解法だと時間計算量はO(N)になる。空間計算量はO(1)になる。

引き継ぐ前に知りたいこと:
* 1つ前を盗んだ場合の最大
* 1つ前を盗まなかった場合の最大

次へ引き継ぐ処理：
* 目の前の家を盗んだ場合の最大 = 1つ前を盗まなかった最大 + 現在の数字
* 目の前の家を盗まなかった場合の最大 = 1つ前を盗まなかった場合の最大 + 1つ前を盗んだ場合の最大

変数増やしたら認知負荷下がるかなと思ったけど、どうだろう。あまり変化を感じない。
```cpp
class Solution {
 public:
  int rob(std::vector<int>& nums) {
    int prev_robbed = 0;
    int prev_skipped = 0;
    for (int num : nums) {
      int robbed = prev_skipped + num;
      int skipped = std::max(prev_skipped, prev_robbed);
      prev_robbed = robbed;
      prev_skipped = skipped;
    }
    return std::max(prev_skipped, prev_robbed);
  }
};
```

かかった時間:
* step1
  * 25min
* step2
  * 3min
* step3
  * 1min
  * 1min
