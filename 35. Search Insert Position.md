## Step1

解法は二分探索を使うことが明らかだったので、実装をするだけだった。
しかし、思ったよりもバグが出てしまい、時間がかなりかかった。
理解したつもりでも、実際にやってみるのは違うと感じた。
考え方は、[start, end)の未探索の範囲を狭めていくイメージ。

```cpp
class Solution {
  int FindInsertPosition(const std::vector<int>& nums, int start, int end,
                         int target) {
    if (start >= end) {
      return start;
    }
    int mid = start + (end - start) / 2;
    if (nums[mid] == target) {
      return mid;
    }
    if (nums[mid] > target) {
      return FindInsertPosition(nums, 0, mid, target);
    }
    return FindInsertPosition(nums, mid + 1, end, target);
  }

 public:
  int searchInsert(std::vector<int>& nums, int target) {
    return FindInsertPosition(nums, 0, nums.size(), target);
  }
};
```

再帰の上限の求め方：
再帰の上限回数 = (スタックサイズ) / (スタックフレームのサイズ)
スタックサイズは, `ulimit -a` (自分のPC)で調べたところ8MBくらい。
スタックフレームのサイズは、`-fstack-usage`フラグを付けてコンパイルをすることでわかる。
しかし、毎度コンパイルして確かめるのは面倒なので、推測してみる。
total = (return address + base pointer) + arguments + local variables (bytes)
      = (8 + 8) + (nums(reference) + start + end + target) + mid
      = 16 + (8 + 4 + 4 + 4) + 4
      = 40 bytes
16の倍数でアライメントされるらしいので、48 bytes になる。

再帰の上限回数 = 8MB / 40B = 2.0 * 10^5 回数再帰できる。
二分探索で、この回数再帰できる配列の最大サイズは、2^k - 1 (k:比較回数)で、 問題の制約である10^4より大きい。


## Step2

* https://github.com/Shunii85/arai60/pull/41/changes
* 反復で書いている。再帰の深さを気にしなくていいので、こちらの方が良い。

* https://nuc.hatenadiary.org/entry/2025/11/29/#%E4%BA%8C%E5%88%86%E6%8E%A2%E7%B4%A2%E3%82%92%E8%AA%AD%E3%82%81%E3%82%8B%E3%81%8B
* 読んだ

* https://github.com/hemispherium/LeetCode_Arai60/pull/39/changes
* `if (nums[mid] == target)` でreturnせずに狭めていく方法もある

(start, end] で書こうとしたら、全然書けなかった。

未探索範囲の設定：
0(left) ... nums.size() - 1(right)
配列の要素を探してるイメージ。leftを挿入位置にするイメージ。

0(start) ... nums.size()(end)
挿入位置を探しているイメージ。start==endが挿入位置になるようにする。

範囲 [left, right] の実装
```cpp
class Solution {
 public:
  int searchInsert(std::vector<int>& nums, int target) {
    int left = 0;
    int right = nums.size() - 1;
    // range: [left, right]
    while (left <= right) {
      int mid = left + (right - left) / 2;
      if (nums[mid] >= target) {
        right = mid - 1;
      } else {
        left = mid + 1;
      }
    }
    return left;  // nums[right] is smaller than target
  }
};
```

範囲 [start, end) の実装
```cpp
class Solution {
 public:
  int searchInsert(std::vector<int>& nums, int target) {
    int start = 0;
    int end = nums.size();
    // range: [start, end)
    while (start < end) {
      int mid = start + (end - start) / 2;
      if (nums[mid] >= target) {
        end = mid;
      } else {
        start = mid + 1;
      }
    }
    return start;  // start == end
  }
};
```

## Step3

```cpp
class Solution {
 public:
  int searchInsert(std::vector<int>& nums, int target) {
    int start = 0;
    int end = nums.size();
    while (start < end) {
      int mid = start + (end - start) / 2;
      if (nums[mid] >= target) {
        end = mid;
      } else {
        start = mid + 1;
      }
    }
    return start;  // start == end
  }
};
```

かかった時間:
* step1
  * 15min
* step2
  * 4min [left, right]
  * 3min [start, end)
* step3
  * 2min
  * 1min
  * 1min
